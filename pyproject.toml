[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = ["live: uses Docker, Qdrant or paid OpenAI calls (explicit opt-in)"]

[tool.ruff]
line-length = 110
target-version = "py312"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
